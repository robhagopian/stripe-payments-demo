spec:
  name: stripe-payments-demo
  services:
  - name: stripe-payments-demo2
    environment_slug: python
    git:
      branch: master
      repo: robhagopian/stripe-payments-demo
  databases:
  - name: example-db
