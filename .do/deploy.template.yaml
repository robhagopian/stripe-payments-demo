spec:
 name: stripe-payments-demo
 services:
  name: sample-python
  - environment_slug: python
  git:
    branch: master
    deploy_on_push: true
    repo: robhagopian/stripe-payments-demo
  databases:
  - name: example-db
